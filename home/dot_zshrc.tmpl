{{ template "commonrc" . }}

if [[ -z $TMUX ]] && ! chezmoi update
then
  echo "Press enter to continue"
  read -s
fi

fpath=(~/.nix-packages/share/zsh/site-functions $fpath)

DISABLE_AUTO_UPDATE="true"

export ZSH="{{ .chezmoi.workingTree }}/ohmyzsh"
source $ZSH/zshrc
